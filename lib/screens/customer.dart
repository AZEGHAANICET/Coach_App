import 'package:flutter/material.dart';
import 'package:flutter_coach_app/main.dart';

class Customer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Customer'),
      ),
      body: Center(child: Text('Customer')),
    );
  }
}
